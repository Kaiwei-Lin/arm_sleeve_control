# Compatibility entry point for the requested filename.
# Install from the repository root: python -m pip install -r requienment.txt
# Real Aurora support additionally uses requirements-aurora.txt.
-r requirements.txt
